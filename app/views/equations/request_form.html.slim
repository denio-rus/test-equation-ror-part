.row.justify-content-center
  .card.col-sm-8
    .card-header.text-center
      h1 Params of an equation
    .card-body
      h3 Lets solve your equation.

      p 
        | First step: choose the type of your equation.  
        =link_to 'linear', '#', class: 'btn btn-outline-primary', id: 'type-switch', remote: true

      p Second step: Enter parameters of the equation.

      = form_with model: @equation, url: solve_equations_path, method: :get, class: 'form-linear' do |f|
        strong Your equation looks like this: ax + b = 0

        = f.hidden_field :type, value: "linear"
        .input-group
          .input-group-prepend
            span.input-group-text id="inputGroup-sizing-default" a:
          = f.text_field :a_param, class: 'form-control col-sm-6 field'
        .input-group
          .input-group-prepend
            span.input-group-text id="inputGroup-sizing-default" b:
          = f.text_field :b_param, class: 'form-control col-sm-6 field'
        br
        = f.submit "Solve", class: 'btn btn-outline-info w-100'

      = form_with model: @equation, url: solve_equations_path, method: :get, class: 'form-quadratic hide' do |f|
        strong Your equation looks like this: ax<sup>2</sup> + bx + c = 0

        = f.hidden_field :type, value: "quadratic"
        .input-group
          .input-group-prepend
            span.input-group-text id="inputGroup-sizing-default" a:
          = f.text_field :a_param, class: 'form-control col-sm-6 field'
        .input-group
          .input-group-prepend
            span.input-group-text id="inputGroup-sizing-default" b:
          = f.text_field :b_param, class: 'form-control col-sm-6 field'
        .input-group
          .input-group-prepend
            span.input-group-text id="inputGroup-sizing-default" c:
          = f.text_field :c_param, class: 'form-control col-sm-6 field'
        br
        = f.submit "Solve", class: 'btn btn-outline-info w-100'
    
    .params-errors  
      strong= render 'errors', resource: @equation
    
    h3.card-header.equation-roots.hide

    .card-footer
      = link_to "Reset", '#', class: 'btn btn-outline-danger w-100', id: 'reset-btn'
    
